name: Souloc Master 🌐

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

concurrency:
  group: souloc-m3u
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🧩 Build souloc.m3u (uses base.txt sources)
        shell: bash
        env:
          TZ: "Asia/Kolkata"
        run: |
          set -euxo pipefail

          tmpdir=$(mktemp -d)
          cleanup() { rm -rf "$tmpdir"; }
          trap cleanup EXIT

          timestamp=$(date +"%Y-%m-%d %H:%M:%S %Z")
          out_file="souloc.m3u"

          echo "🔹 Starting Souloc M3U build at $timestamp"

          # ===== SOURCE CHECK =====
          if [ ! -s base.txt ]; then
            echo "❌ base.txt missing or empty — cannot proceed."
            exit 1
          fi

          grep -v '^\s*#' base.txt | sed '/^[[:space:]]*$/d' > "$tmpdir/sources.txt"

          if [ ! -s "$tmpdir/sources.txt" ]; then
            echo "❌ No valid URLs in base.txt — aborting."
            exit 1
          fi

          echo "🌐 Using sources from base.txt:"
          cat "$tmpdir/sources.txt"

          # ===== FETCH ALL SOURCES =====
          i=0
          while IFS= read -r url || [ -n "$url" ]; do
            [ -z "$url" ] && continue
            i=$((i+1))
            {
              echo "🔹 Fetching: $url"
              if curl -fsSL --max-time 60 --retry 2 "$url" | grep -v '^#EXTM3U' > "$tmpdir/part_$i.m3u"; then
                echo "✅ Success: $url"
              else
                echo "⚠️ Failed: $url"
                : > "$tmpdir/part_$i.m3u"
              fi
            } &
            ((i%8==0)) && wait
          done < "$tmpdir/sources.txt"
          wait

          cat "$tmpdir"/part_*.m3u > "$tmpdir/all.m3u" || true

          echo "🧹 Removing duplicates..."
          awk '
            BEGIN { ch=0 }
            /^#EXTINF/ {
              info=$0; gsub(/[[:space:]]+/, " ", info)
              getline url; gsub(/[[:space:]]+/, "", url)
              if (url=="") next
              key = info "|" url
              if (!(key in seen)) {
                seen[key]=1
                print info; print url
              }
            }
          ' "$tmpdir/all.m3u" > "$tmpdir/all_unique.m3u"

          total=$(grep -c '^#EXTINF' "$tmpdir/all.m3u" || echo 0)
          updated=$(grep -c '^#EXTINF' "$tmpdir/all_unique.m3u" || echo 0)

          # ===== HEADER =====
          {
            echo "# 🔹 Pushed and Updated by Kittujk"
            echo "# 🔸 Coded & Maintained by @RJMBTS"
            echo "# 🕒 Last updated: $timestamp"
            echo "# 📺 Channels : Total - $total | Updated - $updated"
            echo ""
            echo '#EXTM3U billed-msg="RJM Tv - RJMBTS Network"'
        
          } > "$out_file"

          echo "📊 Splitting and sorting by language..."
          mkdir -p "$tmpdir/langs"

          awk -v tmpdir="$tmpdir/langs" '
            /^#EXTINF/ {
              info=$0
              lang="Unknown"
              if (match(info, /group-title="([^"]+)"/, a)) lang=a[1]
              getline url
              gsub(/^[ \t\r\n]+|[ \t\r\n]+$/, "", url)
              if (url=="") next
              file = tmpdir "/" lang ".m3u"
              print info > file
              print url  > file
            }
          ' "$tmpdir/all_unique.m3u"

          declare -A flags=(
            ["Telugu"]="🇮🇳"
            ["Hindi"]="🇮🇳"
            ["Tamil"]="🇮🇳"
            ["Malayalam"]="🇮🇳"
            ["Kannada"]="🇮🇳"
            ["English"]="🇬🇧"
            ["Sports"]="🏆"
            ["Kids"]="🧒"
            ["Music"]="🎵"
            ["Movies"]="🎬"
            ["News"]="🗞️"
            ["Unknown"]="❓"
          )

          echo "Language,Channels" > "$tmpdir/lang_stats.csv"
          for f in "$tmpdir/langs"/*.m3u; do
            lang=$(basename "$f" .m3u)
            count=$(grep -c '^#EXTINF' "$f" || echo 0)
            echo "$lang,$count" >> "$tmpdir/lang_stats.csv"
          done

          echo "🔠 Sorting A–Z inside each language..."
          tail -n +2 "$tmpdir/lang_stats.csv" | sort -t, -k2 -nr | while IFS=, read -r lang num; do
            file="$tmpdir/langs/$lang.m3u"
            [ ! -f "$file" ] && continue
            flag="${flags[$lang]:-🌐}"

            echo "" >> "$out_file"
            echo "# ---------------------------" >> "$out_file"
            echo "# ▶️ $flag $lang Channels (A–Z)" >> "$out_file"
            echo "# ---------------------------" >> "$out_file"

            awk '
              /^#EXTINF/ {info=$0; getline url; data[info]=url}
              END {
                n=asorti(data, sorted)
                ch=1
                for (i=1; i<=n; i++) {
                  info=sorted[i]; url=data[info]
                  if (info !~ /tvg-chno=/)
                    sub(/^#EXTINF:-1/, sprintf("#EXTINF:-1 tvg-chno=\"%d\"", ch), info)
                  print info; print url; ch++
                }
              }
            ' "$file" >> "$out_file"

            echo "" >> "$out_file"
          done

          echo "✅ Souloc M3U built successfully using base.txt sources!"
          echo "📺 Total: $total | ♻️ Unique: $updated"

          {
            echo "## 🌐 Souloc Master — RJMBTS Auto-Build Summary"
            echo "| Metric | Count |"
            echo "|--------|--------:|"
            echo "| 📺 Total fetched | ${total:-0} |"
            echo "| ♻️ Unique (updated) | ${updated:-0} |"
            echo ""
            echo "🕒 **Last updated:** $timestamp"
            echo ""
            echo "🔸 **Coded & Maintained by [@RJMBTS](https://github.com/RJMBTS)**"
            echo "🔹 **Powered by RJM Tv & Souloc Network**"
          } >> $GITHUB_STEP_SUMMARY

      - name: 🚀 Commit & Push souloc.m3u (RJMBTS Auto)
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name "RJMBTS Auto Builder"
          git config user.email "actions@github.com"
          git add souloc.m3u || true

          if git diff --cached --quiet; then
            echo "No new changes — skipping commit."
          else
            git commit -m "🔄 Auto-update souloc.m3u (RJMBTS Network)"
            git pull --rebase origin main || git rebase --abort
            git push origin main || echo "⚠️ Push failed but build succeeded."
          fi

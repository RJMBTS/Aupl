name: Souloc Master 🌐

on:
  schedule:
    - cron: "*/30 * * * *"   # ⏰ Runs every 30 minutes
  workflow_dispatch:          # Manual trigger option

concurrency:
  group: souloc-m3u
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🧩 Build souloc.m3u (RJMBTS Edition)
        run: |
          set -euxo pipefail

          timestamp=$(TZ="Asia/Kolkata" date +"%Y-%m-%d %H:%M:%S %Z")
          tmpdir=$(mktemp -d)
          echo "🔹 Starting Souloc M3U build at $timestamp"

          # ===== HEADER =====
          {
            echo "# 🔹 Souloc M3U — Powered by RJMBTS Network"
            echo "# 🔸 Coded & Maintained by @RJMBTS"
            echo "# 🔹 Auto-generated via GitHub Actions (Souloc Master)"
            echo "# 🕒 Last updated: $timestamp"
            echo ""
            echo '#EXTM3U billed-msg="RJM Tv - RJMBTS Network"'
          } > souloc.m3u
          # ===================

          # ===== SOURCE SETUP =====
          if [ ! -f base.txt ]; then
            echo "⚠️ base.txt not found — using default RJMBTS source."
            echo "https://raw.githubusercontent.com/mrr0b0t25/m3u/main/local.m3u" > base.txt
          fi

          echo "🌐 Reading sources from base.txt..."
          grep -v '^#' base.txt | grep -v '^$' > "$tmpdir/sources.txt"

          if [ ! -s "$tmpdir/sources.txt" ]; then
            echo "❌ No valid URLs in base.txt — stopping after header creation."
            exit 0
          fi

          echo "🌐 Fetching M3U sources in parallel..."
          i=0
          while read -r url; do
            [[ -z "$url" ]] && continue
            i=$((i+1))
            {
              echo "🔹 Fetching: $url"
              if curl -fsSL --max-time 60 "$url" | grep -v '^#EXTM3U' > "$tmpdir/part_$i.m3u"; then
                echo "✅ Success: $url"
              else
                echo "⚠️ Failed: $url"
                echo "" > "$tmpdir/part_$i.m3u"
              fi
            } &
          done < "$tmpdir/sources.txt"
          wait

          echo "✅ All sources fetched. Combining..."
          cat "$tmpdir"/part_*.m3u > "$tmpdir/all.m3u" || true

          echo "🧹 Removing duplicates (based on #EXTINF or URL)..."
          awk '
            /^#EXTINF/ {
              gsub(/[[:space:]]+/, " ", $0)
              info=$0
              getline url
              gsub(/[[:space:]]+/, "", url)
              if (url == "") next
              if (!(info in seen_info) && !(url in seen_url)) {
                seen_info[info]=1
                seen_url[url]=1
                print info
                print url
              }
            }
          ' "$tmpdir/all.m3u" >> souloc.m3u

          total=$(grep -c '^#EXTINF' "$tmpdir/all.m3u" || true)
          unique=$(grep -c '^#EXTINF' souloc.m3u || true)

          # ===== CHANGE TRACKING =====
          current_list="$tmpdir/current_channels.txt"
          grep '^#EXTINF' souloc.m3u > "$current_list"

          if [ -f .souloc_previous.txt ]; then
            old_count=$(grep -c '^#EXTINF' .souloc_previous.txt || true)
            new_added=$(grep -Fvx -f .souloc_previous.txt "$current_list" | grep -c '^#EXTINF' || true)
            updated_count=$((unique - new_added))
          else
            old_count=0
            new_added=$unique
            updated_count=0
          fi

          cp "$current_list" .souloc_previous.txt

          echo "✅ Souloc M3U successfully built under RJMBTS Network."
          echo "📊 Total fetched: $total"
          echo "📊 Unique channels: $unique"
          echo "🆕 New channels: $new_added"
          echo "♻️ Updated channels: $updated_count"

          # ===== MARKDOWN SUMMARY =====
          {
            echo "## 🌐 Souloc Master — RJMBTS Auto-Build Summary"
            echo "| Metric | Count |"
            echo "|--------|--------|"
            echo "| Total fetched | $total |"
            echo "| Unique channels | $unique |"
            echo "| 🆕 New | $new_added |"
            echo "| ♻️ Updated | $updated_count |"
            echo ""
            echo "🕒 **Last updated:** $timestamp"
            echo ""
            echo "🔸 **Coded & Maintained by [@RJMBTS](https://github.com/RJMBTS)**"
            echo "🔹 **Powered by RJM Tv & Souloc Network**"
          } >> $GITHUB_STEP_SUMMARY

      - name: 🚀 Commit & Push souloc.m3u (RJMBTS Auto)
        run: |
          set -euxo pipefail

          git config user.name "RJMBTS Auto Builder"
          git config user.email "actions@github.com"
          git add souloc.m3u .souloc_previous.txt || true

          if git diff --cached --quiet; then
            echo "No new changes — skipping commit."
          else
            git commit -m "🔄 Auto-update souloc.m3u (RJMBTS Network)"
            echo "📥 Rebasing latest main branch..."
            git pull --rebase origin main || git rebase --abort
            echo "🚀 Pushing changes..."
            git push origin main || echo "⚠️ Push failed, but file built successfully."
          fi

      name: 🧩 Build souloc.m3u (Final Pro — Flags, Sorted A–Z, Header Counts)
      shell: bash
      env:
          TZ: "Asia/Kolkata"
        run: |
          set -euxo pipefail

          tmpdir=$(mktemp -d)
          cleanup() { rm -rf "$tmpdir"; }
          trap cleanup EXIT

          timestamp=$(date +"%Y-%m-%d %H:%M:%S %Z")
          out_file="souloc.m3u"

          echo "🔹 Starting Souloc M3U build at $timestamp"

          # ===== SOURCE SETUP =====
          if [ ! -f base.txt ]; then
            echo "⚠️ base.txt not found — using default RJMBTS source."
            echo "https://raw.githubusercontent.com/mrr0b0t25/m3u/main/local.m3u" > base.txt
          fi

          grep -v '^\s*#' base.txt | sed '/^[[:space:]]*$/d' > "$tmpdir/sources.txt" || true
          [ ! -s "$tmpdir/sources.txt" ] && { echo "❌ No valid URLs — exiting."; exit 0; }

          echo "🌐 Fetching M3U sources..."
          i=0
          while IFS= read -r url || [ -n "$url" ]; do
            [ -z "$url" ] && continue
            i=$((i+1))
            {
              echo "🔹 Fetching: $url"
              if curl -fsSL --max-time 60 --retry 2 "$url" | grep -v '^#EXTM3U' > "$tmpdir/part_$i.m3u"; then
                echo "✅ Success: $url"
              else
                echo "⚠️ Failed: $url"
                : > "$tmpdir/part_$i.m3u"
              fi
            } &
            ((i%8==0)) && wait
          done < "$tmpdir/sources.txt"
          wait

          cat "$tmpdir"/part_*.m3u > "$tmpdir/all.m3u" || true

          echo "🧹 Removing duplicates..."
          awk '
            BEGIN { ch=0 }
            /^#EXTINF/ {
              info=$0; gsub(/[[:space:]]+/, " ", info)
              getline url; gsub(/[[:space:]]+/, "", url)
              if (url=="") next
              key = info "|" url
              if (!(key in seen)) {
                seen[key]=1
                print info; print url
              }
            }
          ' "$tmpdir/all.m3u" > "$tmpdir/all_unique.m3u"

          total=$(grep -c '^#EXTINF' "$tmpdir/all.m3u" || echo 0)
          updated=$(grep -c '^#EXTINF' "$tmpdir/all_unique.m3u" || echo 0)

          echo "📊 Splitting by language..."
          mkdir -p "$tmpdir/langs"

          awk -v tmpdir="$tmpdir/langs" '
            /^#EXTINF/ {
              info=$0
              lang="Unknown"
              if (match(info, /group-title="([^"]+)"/, a)) lang=a[1]
              getline url
              gsub(/^[ \t\r\n]+|[ \t\r\n]+$/, "", url)
              if (url=="") next
              file = tmpdir "/" lang ".m3u"
              print info > file
              print url  > file
            }
          ' "$tmpdir/all_unique.m3u"

          # Flags for sections
          declare -A flags=(
            ["Telugu"]="🇮🇳"
            ["Hindi"]="🇮🇳"
            ["Tamil"]="🇮🇳"
            ["Malayalam"]="🇮🇳"
            ["Kannada"]="🇮🇳"
            ["English"]="🇬🇧"
            ["Sports"]="🏆"
            ["Kids"]="🧒"
            ["Music"]="🎵"
            ["Movies"]="🎬"
            ["News"]="🗞️"
            ["Unknown"]="❓"
          )

          echo "Language,Channels" > "$tmpdir/lang_stats.csv"
          for f in "$tmpdir/langs"/*.m3u; do
            lang=$(basename "$f" .m3u)
            count=$(grep -c '^#EXTINF' "$f" || echo 0)
            echo "$lang,$count" >> "$tmpdir/lang_stats.csv"
          done

          # ===== HEADER WITH TOTALS =====
          {
            echo "# 🔹 Pushed and Updated by Kittujk"
            echo "# 🔸 Coded & Maintained by @RJMBTS"
            echo "# 🕒 Last updated: $timestamp"
            echo "# 📺 Total Channels: $total"
            echo "# ♻️ Unique Channels: $updated"
            echo ""
            echo '#EXTM3U billed-msg="RJM Tv - RJMBTS Network"'
            echo ""
          } > "$out_file"

          # ===== LANGUAGE SUMMARY =====
          {
            echo "# ===== LANGUAGE SUMMARY ====="
            tail -n +2 "$tmpdir/lang_stats.csv" | sort -t, -k2 -nr | while IFS=, read -r lang num; do
              flag="${flags[$lang]:-🌐}"
              echo "# $flag $lang: $num channels"
            done
            echo ""
            echo "# ===== SORTED CHANNELS BY LANGUAGE ====="
            echo ""
          } >> "$out_file"

          echo "🔠 Sorting A–Z inside each language..."
          tail -n +2 "$tmpdir/lang_stats.csv" | sort -t, -k2 -nr | while IFS=, read -r lang num; do
            file="$tmpdir/langs/$lang.m3u"
            [ ! -f "$file" ] && continue
            flag="${flags[$lang]:-🌐}"

            echo "" >> "$out_file"
            echo "# ---------------------------" >> "$out_file"
            echo "# ▶️ $flag $lang Channels (A–Z)" >> "$out_file"

name: Souloc Master

on:
  schedule:
    - cron: "30 18 * * *"   # ⏰ Runs daily at midnight IST (18:30 UTC)
  workflow_dispatch:

concurrency:
  group: souloc-m3u
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ✅ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # ✅ Normalize base.txt (convert CRLF → LF)
      - name: Normalize base.txt (convert CRLF → LF)
        run: sed -i 's/\r$//' base.txt || true

      # ✅ Merge, deduplicate, and build playlist
      - name: Combine M3U files
        id: combine
        shell: bash
        run: |
          set -e
          TIME_IST=$(TZ="Asia/Kolkata" date +"%Y-%m-%d %H:%M:%S IST")
          TMP_FILE="souloc_temp.m3u"
          FINAL_FILE="souloc.m3u"

          > "$TMP_FILE"
          echo "🔗 Merging playlists from $(grep -cvE '^\s*#|^\s*$' base.txt) sources..."

          COUNT=0
          grep -vE '^\s*#|^\s*$' base.txt | while read -r url; do
            url="$(echo -n "$url" | tr -d '\r')"
            if [ -n "$url" ]; then
              echo "Fetching: $url"
              if curl -sL --max-time 30 --retry 3 --retry-delay 5 "$url" -o temp_chunk.m3u; then
                cat temp_chunk.m3u >> "$TMP_FILE"
                echo "" >> "$TMP_FILE"
                COUNT=$((COUNT+1))
              else
                echo "⚠️ Skipped unreachable URL: $url"
              fi
            fi
          done

          echo "✅ Total source URLs processed: $COUNT"

          TOTAL_CHANNELS=$(grep -c '^#EXTINF' "$TMP_FILE" || true)
          echo "✅ Channels before deduplication: $TOTAL_CHANNELS"

          # 🚨 Fail if no channels found
          if [ "$TOTAL_CHANNELS" -eq 0 ]; then
            echo "::error::No channels found — all sources may be offline."
            exit 1
          fi

          # ✅ Deduplicate by channel name + URL
          echo "🔄 Removing duplicate channels..."
          awk -v RS="" '
            {
              name=""; url="";
              for (i = 1; i <= NF; i++) {
                if ($i ~ /^#EXTINF/) {
                  match($i, /,(.*)$/, arr)
                  name = arr[1]
                } else if ($i ~ /^http/) {
                  url = $i
                }
              }
              key = name "|" url
              if (!(key in seen)) {
                seen[key] = 1
                print $0 "\n"
              }
            }
          ' "$TMP_FILE" > "souloc_deduped.m3u"

          DEDUPED_COUNT=$(grep -c '^#EXTINF' souloc_deduped.m3u || true)
          echo "✅ Channels after deduplication: $DEDUPED_COUNT"
          mv souloc_deduped.m3u "$TMP_FILE"

          # Since no backup file, treat all as new when first run
          UPDATED_CHANNELS=0
          NEW_CHANNELS=$DEDUPED_COUNT
          REMOVED_CHANNELS=0

          echo "🆕 Newly Added Channels: $NEW_CHANNELS"
          echo "🔁 Updated Channels: $UPDATED_CHANNELS"
          echo "🗑️ Removed Channels: $REMOVED_CHANNELS"

          # ✅ Build final playlist with full analytics header
          {
            echo '# Pushed & Updated by Kittujk'
            echo '# Coded & Scripted for RJM Tv'
            echo "# Last updated: $TIME_IST"
            echo "# Channels : Total - $DEDUPED_COUNT | Updated - $UPDATED_CHANNELS | Newly Added - $NEW_CHANNELS | Removed - $REMOVED_CHANNELS"
            echo ""
            echo '#EXTM3U billed-msg="RJM Tv - RJMBTS Network"'
            echo ""
          } > "$FINAL_FILE"

          cat "$TMP_FILE" >> "$FINAL_FILE"
          echo "# ---- Created Exclusively for RJM Souloc Playlist ----" >> "$FINAL_FILE"

          echo "count=$DEDUPED_COUNT" >> $GITHUB_OUTPUT
          echo "updated=$UPDATED_CHANNELS" >> $GITHUB_OUTPUT
          echo "new=$NEW_CHANNELS" >> $GITHUB_OUTPUT
          echo "removed=$REMOVED_CHANNELS" >> $GITHUB_OUTPUT
          echo "time=$TIME_IST" >> $GITHUB_OUTPUT

          # 🧹 Cleanup temporary files
          rm -f temp_chunk.m3u souloc_deduped.m3u || true

      # ✅ Log playlist statistics
      - name: Log playlist statistics
        if: ${{ always() }}
        run: |
          echo "[${{ steps.combine.outputs.time }}] ▶ Total: ${{ steps.combine.outputs.count }} | 🔁 Updated: ${{ steps.combine.outputs.updated }} | 🆕 New: ${{ steps.combine.outputs.new }} | ❌ Removed: ${{ steps.combine.outputs.removed }}" >> souloc_stats.txt
          # Trim log to last 1000 lines
          tail -n 1000 souloc_stats.txt > souloc_stats.tmp && mv souloc_stats.tmp souloc_stats.txt
          git add -f souloc_stats.txt || true
          echo "🧾 Logged and trimmed playlist statistics."

      # ✅ Verify output
      - name: Verify output
        run: |
          echo "🧾 Generated files:"
          ls -lh souloc.m3u || true
          echo "📄 Preview of souloc.m3u:"
          head -n 25 souloc.m3u || true

      # ✅ Commit only if changes detected
      - name: Commit and push changes
        if: ${{ or(steps.combine.outputs.updated != '0', steps.combine.outputs.new != '0', steps.combine.outputs.removed != '0') }}
        shell: bash
        env:
          GIT_AUTHOR_NAME: "GitHub Actions Bot"
          GIT_AUTHOR_EMAIL: "actions@github.com"
        run: |
          git config user.name "$GIT_AUTHOR_NAME"
          git config user.email "$GIT_AUTHOR_EMAIL"

          git pull --rebase origin main || true
          git add -f souloc.m3u souloc_stats.txt || true

          echo "🧾 Files ready for commit:"
          git status --short

          if ! git diff --quiet --cached; then
            git commit -m "Auto-update Souloc Master Playlist [${{ steps.combine.outputs.time }}] | Total: ${{ steps.combine.outputs.count }} | Updated: ${{ steps.combine.outputs.updated }} | New: ${{ steps.combine.outputs.new }} | Removed: ${{ steps.combine.outputs.removed }}"
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
            echo "✅ Playlist and stats updated successfully."
          else
            echo "ℹ️ No new updates detected — nothing to commit."
          fi

      # 🟡 Skip if no differences
      - name: No changes detected
        if: ${{ and(steps.combine.outputs.updated == '0', steps.combine.outputs.new == '0', steps.combine.outputs.removed == '0') }}
        run: echo "ℹ️ No new, updated, or removed channels detected — skipping commit."
